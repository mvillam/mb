name: mb
description: MiniBattle game
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flame: ^1.10.0
  flame_tiled: ^1.15.0
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner:  ^2.4.6
  flutter_gen: ^5.3.2
flutter:
  uses-material-design: true
  assets:
    - assets/tiles/bm_map.tmx
    - assets/images/tile.png

